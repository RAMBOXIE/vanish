# SCAN_HANDOFF

State: **SCAN_HANDOFF**

What would you like to do next?

- `cleanup` — Continue to the cleanup wizard to remove your data from exposed brokers
- `export` — Just export the scan report and finish (no removal actions)
- `done` — Exit without any further action

Allow commands: `status` / `back` / `pause` / `resume`.
