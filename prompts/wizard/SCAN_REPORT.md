# SCAN_REPORT

State: **SCAN_REPORT**

## Privacy Score: {{privacy_score}}/100 ({{risk_level}})

**Likely exposed**: {{likely_count}} brokers

**Top recommendation**: {{top_recommendation}}

---

Reply `reviewed` or `YES` to acknowledge you've reviewed the report and continue to the handoff decision.

For a full Markdown report:
```bash
node scripts/scan-demo.mjs --name "Your Name" --output-md ./scan-report.md
```

Allow commands: `status` / `back` / `pause` / `resume`.
